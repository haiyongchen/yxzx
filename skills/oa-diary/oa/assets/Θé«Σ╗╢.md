# OA 接口清单 - 邮件

> 分类 GUID: `44125727-1bc5-4e24-b5a5-25e7499abbd5`

## 编辑标签

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_tagedit_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_tagedit_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `tagguid` | STRING | 是 | 要修改的标签标识 |
| `order` | NUMBER | 否 | 修改后的排序值 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"tagguid": "tagguid-1", "order": "1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 还原邮件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_restore_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_restore_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguidlist` | STRING | 是 | 多个 guid 用英文分号 ; 分隔 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguidlist": "mailguidlist-1;mailguidlist-2"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 新增抄送人

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_csaddreceiver_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_csaddreceiver_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 目标邮件 guid |
| `userlist` | STRING | 是 | 多人之间用英文分号 ; 分隔 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1", "userlist": "userlist-1;userlist-2"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 新增收件人

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_addreceiver_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_addreceiver_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 目标邮件 guid |
| `userlist` | STRING | 是 | 多人之间用英文分号 ; 分隔 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1", "userlist": "userlist-1;userlist-2"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 获取邮件详情

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_getdetail_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_getdetail_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 当前邮件 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 删除标签

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_tagdelete_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_tagdelete_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `tagguid` | STRING | 是 | 要删除的标签标识 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"tagguid": "tagguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 新增邮件反馈

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_addfeedback_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_addfeedback_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 目标邮件 guid |
| `notcieotheruser` | STRING | 是 | 1 通知，0 不通知 |
| `feedbackcontent` | STRING | 是 | 支持富文本 HTML 内容 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1", "notcieotheruser": "", "feedbackcontent": "示例内容"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 签收邮件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_signmail_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_signmail_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguidlist` | STRING | 是 | 多个 guid 用英文分号 ; 分隔 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguidlist": "mailguidlist-1;mailguidlist-2"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 修改反馈

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_feedbackedit_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_feedbackedit_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `feedbackguid` | STRING | 是 | 要修改的反馈 guid |
| `feedbackvalue` | STRING | 是 | 修改后的反馈 HTML 内容 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"feedbackguid": "feedbackguid-1", "feedbackvalue": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 收回已签收邮件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_shouhui_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_shouhui_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 要收回的邮件 guid |
| `receiveuserguidlist` | STRING | 是 | 多个 guid 用英文分号 ; 分隔 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1", "receiveuserguidlist": "receiveuserguidlist-1;receiveuserguidlist-2"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 获取邮件标签列表

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_gettaglst_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_gettaglst_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 批量修改标签排序

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_tagbatchedit_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_tagbatchedit_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `taglist` | ARRAY | 是 | 批量排序提交数组 |
| `taglist[].tagguid` | STRING | 是 | 排序对象标识 |
| `taglist[].name` | STRING | 是 | 排序对象名称 |
| `taglist[].order` | NUMBER | 是 | 新的排序值 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"taglist": "taglist-1;taglist-2", "taglist[].tagguid": "taglist[].tagguid-1", "taglist[].name": "示例值", "taglist[].order": "1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 转为我的任务邮件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_convertmytask_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_convertmytask_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 要转换的邮件 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 结束任务邮件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_taskend_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_taskend_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 目标任务邮件 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 未读邮件列表

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_getunreadlist_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_getunreadlist_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

---

## 更新免打扰状态

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_updatenodisturb_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_updatenodisturb_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 当前邮件 guid |
| `nodisturb` | STRING | 是 | 1 开启，0 关闭 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1", "nodisturb": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 保存草稿

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_save_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_save_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `subject` | STRING | 否 | 草稿标题 |
| `touserguidlist` | STRING | 否 | 多个 guid 用英文分号 ; 分隔 |
| `secretuserguidlist` | STRING | 否 | 当前页面常传空字符串 |
| `chaosonguserguidlist` | STRING | 否 | 多个 guid 用英文分号 ; 分隔 |
| `mailcontent` | STRING | 否 | 富文本 HTML 内容 |
| `resave` | STRING | 是 | 1 重新保存，0 普通保存 |
| `mailguid` | STRING | 是 | 已存在草稿传真实 guid，否则传 clientguid |
| `istask` | STRING | 否 | 当前页面固定传 0 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"subject": "示例值", "touserguidlist": "touserguidlist-1;touserguidlist-2", "secretuserguidlist": "secretuserguidlist-1;secretuserguidlist-2", "chaosonguserguidlist": "chaosonguserguidlist-1;chaosonguserguidlist-2", "mailcontent": "示例内容", "resave": "", "mailguid": "mailguid-1", "istask": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 永久删除邮件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_deleteforever_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_deleteforever_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguidlist` | STRING | 是 | 多个 guid 用英文分号 ; 分隔 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguidlist": "mailguidlist-1;mailguidlist-2"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 删除临时附件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `deleteattach_attachguid_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/deleteattach_attachguid_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `attachguid` | STRING | 是 | 要删除的临时附件标识 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"attachguid": "attachguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 邮件转日程

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `schedule_addbyoamark_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/schedule_addbyoamark_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `frompk` | STRING | 是 | 当前邮件 guid |
| `frommark` | STRING | 是 | 当前固定为 mail |
| `isautoconvert` | NUMBER | 否 | 0 否，1 是 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"frompk": "", "frommark": "", "isautoconvert": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 校验邮件查看权限

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_isowner_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_isowner_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `maildetailguid` | STRING | 是 | 分享场景传入的详情 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"maildetailguid": "maildetailguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 获取反馈列表

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_getfeedbacklist_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_getfeedbacklist_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 当前邮件 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 更新邮件关联群

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mailyjjqupdate_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mailyjjqupdate_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 当前邮件 guid |
| `mailgroupid` | STRING | 是 | 即时通信群 id |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1", "mailgroupid": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 邮件列表

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_getlist_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_getlist_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `boxtype` | STRING | 是 | 代码中出现 100000 收件箱、100001 已发送；注释还提到 100002 草稿箱、100003 回收站 |
| `keyword` | STRING | 否 | 搜索关键字 |
| `currentpageindex` | NUMBER | 是 | 分页页码，从 1 开始 |
| `pagesize` | STRING | 是 | 调用处固定传 3 |
| `searchtype` | NUMBER | 是 | 代码中实际出现：0 标题、1 发件人、2 反馈、3 全文检索 |
| `tagguid` | STRING | 否 | 按标签过滤时使用 |
| `isshowforday` | STRING | 否 | 0 不分组，1 按今天/本周/更早分组 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"boxtype": "", "keyword": "测试", "currentpageindex": "1", "pagesize": "1", "searchtype": "", "tagguid": "tagguid-1", "isshowforday": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 获取签收列表

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_signlst_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_signlst_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 要查询签收情况的邮件 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 撤回反馈

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_feedbackdel_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_feedbackdel_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `feedbackguid` | STRING | 是 | 要撤回的反馈 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"feedbackguid": "feedbackguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 获取邮件列表

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_getlist_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_getlist_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `boxtype` | STRING | 是 | 代码中出现 100000 收件箱、100001 已发送；注释还提到 100002 草稿箱、100003 回收站 |
| `keyword` | STRING | 否 | 搜索关键字 |
| `currentpageindex` | NUMBER | 是 | 分页页码，从 1 开始 |
| `pagesize` | STRING | 是 | 调用处固定传 3 |
| `searchtype` | NUMBER | 是 | 代码中实际出现：0 标题、1 发件人、2 反馈、3 全文检索 |
| `tagguid` | STRING | 否 | 按标签过滤时使用 |
| `isshowforday` | STRING | 否 | 0 不分组，1 按今天/本周/更早分组 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"boxtype": "", "keyword": "测试", "currentpageindex": "1", "pagesize": "1", "searchtype": "", "tagguid": "tagguid-1", "isshowforday": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 转任务邮件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_converttask_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_converttask_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 要转换的邮件 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 获取短信提醒配置

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `handle_getsendsmssetting_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/handle_getsendsmssetting_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 发送邮件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_send_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_send_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `subject` | STRING | 是 | 主题，邮件标题 |
| `touserguidlist` | STRING | 是 | 收件人 guid 列表，多个 guid 用英文分号分隔 |
| `secretuserguidlist` | STRING | 否 | 密送人 guid 列表 |
| `chaosonguserguidlist` | STRING | 否 | 抄送人 guid 列表 |
| `mailcontent` | STRING | 是 | 邮件内容，富文本 HTML |
| `outboxguid` | STRING | 是 | 客户端发送 guid，用于关联已上传附件 |
| `ifnotice` | STRING | 否 | 是否发送短信提醒 |
| `istask` | STRING | 否 | 是否任务邮件 |
| `nodisturb` | STRING | 否 | 免打扰标识 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"subject": "邮件主题示例", "touserguidlist": "receiver-guid-1", "secretuserguidlist": "", "chaosonguserguidlist": "", "mailcontent": "<p>邮件正文示例</p>", "outboxguid": "outbox-guid-1", "ifnotice": "0", "istask": "0", "nodisturb": "0"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 新增标签

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_tagadd_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_tagadd_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `order` | NUMBER | 否 | 未传时默认 0 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"order": "1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 给邮件添加标签

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_tagaddrelation_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_tagaddrelation_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguid` | STRING | 是 | 当前邮件 guid |
| `tagguid` | STRING | 是 | 多个标签 guid 用英文分号 ; 分隔 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguid": "mailguid-1", "tagguid": "tagguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

## 获取反馈详情

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_getfeedbackdetail_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_getfeedbackdetail_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `feedbackguid` | STRING | 是 | 要查询的反馈 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"feedbackguid": "feedbackguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 删除邮件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `mail_delete_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/mail_delete_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `mailguidlist` | STRING | 是 | 邮件 guid 列表，多个 guid 用英文分号分隔 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"mailguidlist": "mail-guid-1;mail-guid-2"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}}
```

---

