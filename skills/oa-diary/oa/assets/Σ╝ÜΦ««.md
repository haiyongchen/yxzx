# OA 接口清单 - 会议

> 分类 GUID: `47b199f8-5804-4f5c-849b-1a0d4886c315`

## 获取会议日历面板数据

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `getmeetingbasicinfoboard` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/getmeetingbasicinfoboard` |
| **请求方法** | `POST` |
| **Content-Type** | `application/x-www-form-urlencoded` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `params` | STRING | 是 | 形如 {"timeslot":{"start":"","end":"","title":"","view":""}} |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"params": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 获取会议列表

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `getmeetingbasicinfolist` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/getmeetingbasicinfolist` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `userguid` | STRING | 是 | 当前登录人 guid |
| `first` | NUMBER | 是 | 代码中为 currPage * pageSize |
| `pageSize` | NUMBER | 是 | 当前页面固定 10 |
| `view` | STRING | 否 | 01 主持，02 参与，03 草稿；首页还有空值表示全部 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"userguid": "userguid-1", "first": "", "pageSize": "1", "view": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 获取会议详情

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `getmeetingbasicinfobyrowguid` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/getmeetingbasicinfobyrowguid` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `rowguid` | STRING | 是 | 要查询的会议标识 |
| `operateuserguid` | STRING | 否 | 详情页场景会传入 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"rowguid": "rowguid-1", "operateuserguid": "operateuserguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 获取会议室地点字典

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `getcodeitemlistbycodename` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/getcodeitemlistbycodename` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `codename` | STRING | 是 | 当前页面固定值 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"codename": "示例值"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 更新会议

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `update` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/update` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `rowguid` | STRING | 是 | 编辑场景的会议标识 |
| `operateuserguid` | STRING | 是 | 当前登录人 guid |
| `ouguid` | STRING | 是 | 当前登录人组织 guid |
| `meetingname` | STRING | 是 | 页面校验必填 |
| `modelguid` | STRING | 是 | 页面校验必填 |
| `userguids` | STRING | 否 | 其他模板时使用 |
| `seatguids` | STRING | 否 | 非其他模板时使用 |
| `meetingdate` | STRING | 是 | 页面校验必填 |
| `meetingstarttime` | STRING | 是 | 页面校验必填 |
| `meetingendtime` | STRING | 是 | 页面校验必填 |
| `isremind` | STRING | 否 | 1 开启，0 关闭 |
| `isrepair` | STRING | 否 | 页面固定默认 0 |
| `meetingshape` | STRING | 是 | 多值逗号分隔 |
| `meetingroomguid` | STRING | 否 | 线下公司会议室使用 |
| `meetingroomother` | STRING | 否 | 线上/非公司会议室使用 |
| `meetingexplain` | STRING | 否 | 页面填写说明 |
| `status` | STRING | 是 | 编辑发布场景固定为 2 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"rowguid": "rowguid-1", "operateuserguid": "operateuserguid-1", "ouguid": "ouguid-1", "meetingname": "示例值", "modelguid": "modelguid-1", "userguids": "userguids-1", "seatguids": "seatguids-1", "meetingdate": "2026-03-28", "meetingstarttime": "09:00", "meetingendtime": "10:00", "isremind": "", "isrepair": "", "meetingshape": "", "meetingroomguid": "meetingroomguid-1", "meetingroomother": "", "meetingexplain": "示例内容", "status": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}, "custom": {"msg": ""}}
```

---

## 上传议题附件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `meeting_uploadtopicattach_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/meeting_uploadtopicattach_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `multipart/form-data` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `attachfilename` | STRING | 是 | 附件名称 |
| `contenttype` | STRING | 否 | 内容类型 |
| `documenttype` | STRING | 否 | 文档类型 |
| `clienttag` | STRING | 是 | 业务标记，默认 Meeting_TopicInfo |
| `clientinfo` | STRING | 否 | 业务信息 |
| `clientguid` | STRING | 是 | 当前议题临时 guid |
| `attachguid` | STRING | 否 | 附件 guid |
| `file` | FILE | 是 | 文件二进制 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"attachfilename": "meeting-topic.docx", "contenttype": "", "documenttype": "", "clienttag": "Meeting_TopicInfo", "clientinfo": "", "clientguid": "topic-guid-1", "attachguid": "", "file": "BINARY_FILE_PLACEHOLDER"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}, "custom": {"attachguid": "attach-guid-1"}}
```

---

## 删除议题

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `deletetopic` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/deletetopic` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `rowguid` | STRING | 是 | 要删除的议题标识 |
| `operateuserguid` | STRING | 是 | 当前登录人 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"rowguid": "rowguid-1", "operateuserguid": "operateuserguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}, "custom": {"msg": ""}}
```

---

## 保存议题

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `savetopic` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/savetopic` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `rowguid` | STRING | 是 | 页面先本地生成 uuid |
| `relationguid` | STRING | 是 | 所属会议标识 |
| `operateuserguid` | STRING | 是 | 当前登录人 guid |
| `topicname` | STRING | 是 | 页面校验必填 |
| `topiccontent` | STRING | 是 | 页面校验必填 |
| `isvote` | STRING | 是 | 1 是，0 否 |
| `issign` | STRING | 是 | 1 是，0 否 |
| `sort` | STRING | 否 | 页面默认为 0 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"rowguid": "rowguid-1", "relationguid": "relationguid-1", "operateuserguid": "operateuserguid-1", "topicname": "示例值", "topiccontent": "示例内容", "isvote": "", "issign": "", "sort": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}, "custom": {"msg": ""}}
```

---

## 上传议题附件

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `uploadattach_v7` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/uploadattach_v7` |
| **请求方法** | `POST` |
| **Content-Type** | `multipart/form-data` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `attachfilename` | STRING | 是 | 上传文件名 |
| `contenttype` | STRING | 否 | 页面当前传空字符串 |
| `documenttype` | STRING | 否 | 页面当前传空字符串 |
| `clienttag` | STRING | 是 | 会议议题附件标识 |
| `clientinfo` | STRING | 否 | 页面当前传空字符串 |
| `clientguid` | STRING | 是 | 当前议题临时 guid |
| `attachguid` | STRING | 否 | 新增上传时为空 |
| `file` | FILE | 是 | 实际上传文件，字段名由文件名动态生成 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"attachfilename": "示例值", "contenttype": "示例内容", "documenttype": "", "clienttag": "", "clientinfo": "", "clientguid": "clientguid-1", "attachguid": "attachguid-1", "file": "BINARY_FILE_PLACEHOLDER"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}, "custom": {"attachguid": "attach-guid-1"}}
```

---

## 保存会议

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `save` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/save` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `rowguid` | STRING | 否 | 新增场景可为空 |
| `operateuserguid` | STRING | 是 | 当前登录人 guid |
| `ouguid` | STRING | 是 | 当前登录人组织 guid |
| `meetingname` | STRING | 是 | 页面校验必填 |
| `modelguid` | STRING | 是 | 页面校验必填 |
| `userguids` | STRING | 否 | 其他模板时使用 |
| `seatguids` | STRING | 否 | 非其他模板时使用 |
| `meetingdate` | STRING | 是 | 页面校验必填 |
| `meetingstarttime` | STRING | 是 | 页面校验必填 |
| `meetingendtime` | STRING | 是 | 页面校验必填 |
| `isremind` | STRING | 否 | 1 开启，0 关闭 |
| `isrepair` | STRING | 否 | 页面固定默认 0 |
| `meetingshape` | STRING | 是 | 多值逗号分隔 |
| `meetingroomguid` | STRING | 否 | 线下公司会议室使用 |
| `meetingroomother` | STRING | 否 | 线上/非公司会议室使用 |
| `meetingexplain` | STRING | 否 | 页面填写说明 |
| `status` | STRING | 是 | 1 保存，2 发布 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"rowguid": "rowguid-1", "operateuserguid": "operateuserguid-1", "ouguid": "ouguid-1", "meetingname": "示例值", "modelguid": "modelguid-1", "userguids": "userguids-1", "seatguids": "seatguids-1", "meetingdate": "2026-03-28", "meetingstarttime": "09:00", "meetingendtime": "10:00", "isremind": "", "isrepair": "", "meetingshape": "", "meetingroomguid": "meetingroomguid-1", "meetingroomother": "", "meetingexplain": "示例内容", "status": ""}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}, "custom": {"msg": ""}}
```

---

## 删除会议

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `delete` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/delete` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `rowguid` | STRING | 是 | 要删除的会议标识 |
| `operateuserguid` | STRING | 是 | 当前登录人 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"rowguid": "rowguid-1", "operateuserguid": "operateuserguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": ""}, "custom": {"msg": ""}}
```

---

## 获取会议模板列表

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `getmeetingmodelinfolist` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/getmeetingmodelinfolist` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `userguid` | STRING | 是 | 当前登录人 guid |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"userguid": "userguid-1"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

## 获取可用会议室列表

| 属性 | 内容 |
| :--- | :--- |
| **标识符** | `getmeetingroominfolist` |
| **API 路径** | `http://oa.epoint.com.cn/oaextend/rest/dynamicapi/getmeetingroominfolist` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **描述** | 无 |

#### 请求参数

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `roomname` | STRING | 否 | 搜索框输入内容 |
| `roomplace` | STRING | 否 | 地点筛选值 |
| `rowguid` | STRING | 否 | 编辑场景下用于排除自身占用 |
| `first` | NUMBER | 是 | 代码中为 currPage * pageSize |
| `pageSize` | NUMBER | 是 | 当前页面固定 10 |
| `meetingdate` | STRING | 是 | 筛选条件 |
| `meetingstarttime` | STRING | 是 | 筛选条件 |
| `meetingendtime` | STRING | 是 | 筛选条件 |

#### 响应参数内容

| 参数名 | 类型 | 必填 | 描述 |
| :--- | :--- | :--- | :--- |
| `根节点` | OBJECT | 否 |  |

**请求示例:**
```json
{"roomname": "示例值", "roomplace": "", "rowguid": "rowguid-1", "first": "", "pageSize": "1", "meetingdate": "2026-03-28", "meetingstarttime": "09:00", "meetingendtime": "10:00"}
```

**响应示例:**
```json
{"status": {"code": "1", "text": "请求成功"}, "custom": {}}
```

---

